{
  "name": "linguafix-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173\" \"wait-on tcp:127.0.0.1:5173 && electron .\"",
    "build": "npm --prefix frontend run build && cargo build --release",
    "start": "electron ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^37.7.0",
    "wait-on": "^9.0.3"
  }
}
